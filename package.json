{
  "name": "sample-test-cases",
  "version": "1.0.0",
  "private": true,
  "description": "Sample Markdown test cases + generated Playwright suite for UI Explorer Agent",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
